-extends "msgs/message_box.html"
-load smartmin sms
-load i18n

-block title-icon
  %span.title-icon
    - if current_label.is_folder
      .glyph.icon-folder
    - else
      .glyph.icon-tag

-block buttons-right
  -block gear-menu
    -include "gear_links_include.haml"

-block post-content
  {{ block.super }}
  - if org_perms.msgs.label_delete
    .deletion.hide
      .title
        - if current_label.is_folder
          - trans "Remove folder"
        - else
          - trans "Remove label"

      .body
        %p
          - if current_label.is_folder
            - trans "Are you sure you want to remove this folder? This will also delete any labels in this folder."
          - else
            {% if current_label.dependent_flows.count == 0 %}
            - trans "Are you sure you want to remove this label?"
            {% else %}
            - trans "This label can't be removed because it's in use."
            {% endif %}
        %p
          %b {{ current_label }}
          %p
            {% if current_label.dependent_flows.count == 0 %}
              -trans "Once removed, it will be permanently deleted. This action can't be undone."
            {% else %}
              {% blocktrans trimmed count num_flows=current_label.dependent_flows.count %}
              Used by {{num_flows}} flow:
              {% plural %}
              Used by {{num_flows}} flows:
              {% endblocktrans %}

            %ul
            - for flow in current_label.dependent_flows.all
              %li
                %a{href:"{% url 'flows.flow_editor' flow.uuid %}"}
                  {{ flow.name }}

            {% endif %}

      %a#delete-form.posterize{href:'{% url "msgs.label_delete" current_label.pk %}'}
