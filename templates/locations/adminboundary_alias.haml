-extends "smartmin/read.haml"

-load compress i18n

-block extra-style
  {{block.super}}
  :css
    .page-content {
      align-self: auto;
      max-width: 100%;
    }

    alias-editor {
      --textarea-height: 130px;
    }

-block title
  -blocktrans trimmed with name=object.name
    Aliases for {{ name }}

-block content
  .my-4
    -blocktrans trimmed with brand=brand.name
      Some regions are known by more than one name. In {{ brand }}, these alternate names are called aliases.
    
  <alias-editor endpoint="/adminboundary/" osmid="{{ object.osm_id }}"></alias-editor>

