-extends "channels/channel_claim_form.html"
-load i18n compress temba

-block pre-form
    -blocktrans with name=brand.name trimmed
      With <b>Weni Web Chat</b>, you can integrate your workspace as a chat on your website.
  .mt-2
    -blocktrans trimmed
      After entering a
      .code Name
      for the chat that will connect to your website, you'll need to configure the
      .code Base URL
      required for sending and receiving messages.
      Then you'll need to configure how the chat will be shown on your website.

  .mt-2
    -blocktrans trimmed
      For this configuration, see
        %a(href="https://docs.ilhasoft.mobi/l/en/channels-category/untitled-article-en")
          this documentation.

-block extra-style
  {{ block.super }}

