-extends 'includes/modal.html'
-load i18n temba smartmin

-block modal
  -if object.children.all
    -blocktrans trimmed with name=object.name
      <b>{{name}}</b> can't be deleted since it's a parent to other labels.

  -else
    -blocktrans trimmed with name=object.name
      Are you sure you want to delete <b>{{name}}</b>? This action can't be undone.

    .hidden
      %form(method="POST")
        %input(type="submit" value="{% trans 'Delete' %}")