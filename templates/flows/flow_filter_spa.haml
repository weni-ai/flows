-extends "flows/flow_list_spa.haml"
-load smartmin sms temba compress
-load i18n

-block title-icon
  %span.title-icon
    .glyph.icon-tag

-block above-bar
  -block top-form
    %form{method:"get"}
      %input.input-medium.search-query{type:'text', placeholder:'{% trans "Search" %}', name:"search", value:"{{search}}"}

-block buttons-right
  -block gear-menu
    -include "gear_links_include.haml"
  
-block post-content
  {{ block.super }}
  - if org_perms.flows.flowlabel_delete
    .deletion.hide
      .title
        -trans "Remove label"

      .body
        %p
          -trans "Are you sure you want to remove this label?"
        %p
          %b {{ current_label }}
          %p
            -trans "Once removed, it will be permanently deleted. This action can't be undone."

      %a#delete-form.posterize{href:'{% url "flows.flowlabel_delete" current_label.pk %}'}

    .deletion-fail.hide
      .title
        -trans "Remove label"

      .body
        %p
          -trans "You can't remove a label that has child labels. Remove all child labels first."

