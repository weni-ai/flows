-extends "smartmin/base.html"
-load i18n

-block title
  .page-title
    -trans "Choose workspace"

-block content
  -if not orgs
    .my-6
      -trans "Your login isn't associated with any workspace. Contact your administrator to receive an invitation."

  -for org in orgs
    %a.button-light.mr-3.inline-block.posterize(href="?organization={{ org.id }}")
      {{ org.name }}

-block extra-style
  :css
    .glyph.icon-users {
      margin-top: 30px;
    }