-extends "orgs/login/login_base.haml"
-load smartmin i18n

-block login-title
  -trans "Two-factor authentication"

-block login-content
  .mt-4
    -blocktrans trimmed
      Open the one-time password app you used to set up two-factor authentication on your account and enter the code
      below to verify your identity.

  %form(method="POST")
    -csrf_token

    .mt-4
      %temba-textinput(name='otp' maxlength=6 placeholder='{{_("6-digit code")|escape}}')

    -if form.otp.errors
      .alert-error.mt-4
        {{ form.otp.errors }}

    .mt-2.text-right
      %a(href="{% url 'users.two_factor_backup' %}")
        -trans "Use backup token"

    .mt-6
      %input.button-primary(type="submit" value='{{_("Verify")|escape}}')